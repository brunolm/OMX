if ((/^https?:\/\/www\.orkut.*/i.test(window.location.href) && !/\/_\//.test(window.location.href))
    || /manageraddons.com\/omx/i.test(window.location.href))
{
    if (typeof(unsafeWindow) === "undefined")
        unsafeWindow = window;

unsafeWindow.Manager = {};

function AddManagerOverlay()
{
    if (!document) return;
    if (!document.body) return;
    if (document.getElementById("manager-overlay")) return;
    var overlay = document.createElement("div");
    overlay.id = "manager-overlay";
    overlay.className = "manager-overlay";
    overlay.style.display = "none";
    overlay.addEventListener("click", function() {
        overlay.style.display = "none";
        [].forEach.call(document.querySelectorAll(".manager-layered"), function(el) { el.style.display = "none"; });
        [].forEach.call(document.querySelectorAll(".manager-layered-remove"), function(el) { try { el.parentNode.removeChild(el); } catch (ex) { } });
    }, false);
    unsafeWindow.Manager.Overlay = overlay;
    document.body.appendChild(overlay);
}

window.addEventListener("DOMContentLoaded", function() {
    AddManagerOverlay();
}, false);

unsafeWindow.setTimeout(function() {
    AddManagerOverlay();
}, 300);